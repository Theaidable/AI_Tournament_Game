using System;
using AIGame.Core;
using UnityEngine;

namespace #NAMESPACE#
{
    /// <summary>
    /// Factory that spawns #SCRIPTNAME# agents.
    /// Creates a full team of agents using custom AI behaviour.
    /// </summary>
    [RegisterFactory("Your Factory Name")]
    public class #SCRIPTNAME# : AgentFactory
    {
        /// <summary>
        /// Returns the agent types this factory wants to spawn.
        /// </summary>
        /// <returns>An array containing the AI types to spawn.</returns>
        protected override System.Type[] GetAgentTypes()
        {
            return new System.Type[] { typeof(YourAIClass) };
        }
    }
}